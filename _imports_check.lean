import Mathlib.Tactic
import Mathlib.Data.Real.Basic
import Mathlib.Data.Nat.Basic
import Mathlib.Probability.Distributions.Gaussian.Real

theorem _probe_ (α β : ℝ) : (sin α) ^ 2 - (sin β) ^ 2 = sin (α + β) * sin (α - β) := by
  sorry
